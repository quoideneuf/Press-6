version: "3.4"

services:
  app:
    build:
      context: .docker/ci-app
      dockerfile: Dockerfile.ci
    ports:
      - "3000"
    environment:
      BUNDLE_PATH: /app/vendor/bundle
    entrypoint:
      - "tail"
      - "-f"
      - /dev/null
